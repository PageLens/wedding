<%- headers = ['Reservation ID', 'Name', 'Email', 'State', 'Dietary'] -%>
<%= CSV.generate_line headers %>
<%- @guests.each do |guest| -%>
  <%= CSV.generate_line([guest.reservation_id, guest.name, guest.email, guest.state, guest.dietary]) %>
<%- end -%>
